# Bumped whenever normalize.py, the rule pack, fusion thresholds' shape,
# or dedupe change in a way that could change a run's outcome — stamped
# onto every ModerationRun so a past decision can always be traced back
# to the exact code that produced it.
ENGINE_VERSION = "2026.1"
